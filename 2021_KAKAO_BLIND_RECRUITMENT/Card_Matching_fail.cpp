#include <iostream>
#include <string>
#include <vector>

int solution(std::vector<std::vector<int>> board, int r, int c) {
    int answer = 0;
    return answer;
}

int main(){
    std::vector<std::vector<int>> board = {{1,0,0,3},{2,0,0,0},{0,0,0,2},{3,0,1,0}};
    int r = 1, c = 0;
    std::cout << solution(board,r,c);
}